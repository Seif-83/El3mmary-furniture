// Firebase is not used in this project. Provide lightweight stubs
// so TypeScript imports don't fail when the package is not installed.

const auth: null = null;
const db: null = null;
let analytics: any = null;

export { auth, db, analytics };
export default null;
